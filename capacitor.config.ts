import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'eccomerce_pediaApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
